# 2주차 실습 기록 — (본인 이름)

> 이 파일을 `records/본인이름.md`로 복사해서 채운 뒤 커밋·푸시한다.
> 스크린샷은 이 저장소에 이미지로 올려서 넣거나, 명령 출력은 코드블록으로 붙여넣어도 된다.

## 실습 환경

- OS:
- Docker 버전 (`docker --version`):

---

## STEP 2. 이미지 빌드

`docker images | findstr petclinic` 결과 (스크린샷 또는 출력):

```
(여기에 붙여넣기)
```

---

## STEP 3~4. 실행 + 브라우저 확인

- `docker ps` 결과 (STATUS와 PORTS가 보이게):

```
(여기에 붙여넣기)
```

- http://localhost:8081 PetClinic 첫 화면 스크린샷:

  (이미지)

- http://localhost:8081/actuator/health 결과:

```
(예: {"status":"UP"})
```

**❓ 생각 질문**: `docker ps`가 `Up`인데도 `/actuator/health`가 바로 `UP`이 아닐 수 있다. 왜 그런가? (컨테이너 상태와 앱 상태의 차이로 설명)

> (답변)

---

## STEP 5. 관찰 3종

- `docker logs`에서 찾은 `Started PetClinicApplication ...` 라인:

```
(붙여넣기)
```

- `docker stats`에서 본 `MEM USAGE / LIMIT` (LIMIT이 1GiB 근처인지):

```
(붙여넣기)
```

- `docker inspect --format '{{.HostConfig.RestartPolicy.Name}}' petclinic-app` 결과:

```
(예: unless-stopped)
```

---

## STEP 6. 강제 종료 → 자동 재시작

- `docker kill` 후 잠시 뒤 `docker ps` (다시 `Up`으로 살아났는지):

```
(붙여넣기 — STATUS 시간이 리셋됐으면 재시작된 것)
```

**❓ 질문 1**: `docker kill`과 `docker stop`의 차이는? 그리고 `--restart unless-stopped` 상태에서 둘의 결과가 어떻게 달랐나?

> (답변)

**❓ 질문 2**: `always`와 `unless-stopped`는 무엇이 다른가?

> (답변)

---

## STEP 7. save / load (이식성)

- 만든 tar 파일명과 대략 크기:

```
(예: spring-petclinic-1.0.tar, 약 350MB)
```

- (짝꿍 컴퓨터 또는 이미지 삭제 후 재로드해) `load` → `run` 성공 스크린샷:

  (이미지)

**❓ 질문**: 소스코드도 자바 설치도 없이 tar 파일 하나로 서버가 뜬 이유는? (이미지 안에 무엇이 들어 있길래?)

> (답변)

---

## STEP 7 (선택). Docker Hub push/pull

했다면 결과 기록:

```
(push/pull 명령과 결과)
```

---

## 오늘 배운 것 한 줄 정리

> (자유롭게)

## 막혔던 점 / 트러블슈팅 기록

> (있으면 — 다음 사람에게 도움이 된다)
